@model IEnumerable<OfferServiceViewModel>

@{
    ViewBag.Title = "Review Offers";
}

<h2 class="text-center">@ViewBag.Title</h2>
<hr />
@if (Model.Any())
{
@foreach(var offer in @Model){

<div class="d-flex justify-content-center">
    <div class="card " style="width: 50rem;">
        <div class="card-body">
            <h3 class="card-title text-center">@offer.Price</h3>
            <p class="card-text"><span class="fw-bold">OwnerId: </span>@offer.OwnerId</p>
            <p class="card-text"><span class="fw-bold">JobId: </span>@offer.JobId</p>
            <p class="card-text"><span class="fw-bold">OfferId: </span>@offer.Id</p>
            <p class="card-text"><span class="fw-bold">Description: </span>@offer.Description</p>
            <a asp-controller="Offer" asp-action="Accept" asp-route-id="@offer.Id" class="btn btn-success">Accept</a>
            <a asp-controller="Offer" asp-action="Decline" asp-route-id="@offer.Id" class="btn btn-danger">Decline</a>


            @*@if (Model.OwnerId == this.User.Id())
            {
                   <a asp-controller="Job" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Edit</a>
                <a asp-controller="Job" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger">Delete</a>
                <tr class="actions">
            <th><span class="fw-bold">Actions:</th>

            <td>
            <a asp-controller="Job" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Edit</a>
            <a asp-controller="Job" asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger">Delete</a>
            </td>
            </tr>}*@ 
        </div>
    </div>
</div>
}    
}
else
{
 <h3 class="text-center">No offers</h3>
}